[build]
builder = "RAILPACK"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$healthcheckPort --workers 2 wsgi:app"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
healthcheckPort = 5001
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10