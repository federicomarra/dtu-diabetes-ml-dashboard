[build]
builder = "RAILPACK"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:8000 --workers 2 wsgi:app"
# healthcheckPath = "/api/health"
# healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10